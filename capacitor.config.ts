import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'radhouen.recipe.app',
  appName: 'recipe-app',
  webDir: 'dist/recipe-app',
  bundledWebRuntime: false
};

export default config;
